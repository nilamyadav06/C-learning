#include <stdio.h>

int main()
{
  // Q1. write a program to calculate average of three integer.Number are given by the user.
  /*
  int a, b, c;
  printf("Enter a three integer");
  scanf("%d %d %d", &a, &b, &c);
  float avg = (a + b + c) / 3.0;
  printf("Average of %d , %d and %d is %0.2f", a, b, c, avg);
  */

  // Q2. write a program to calculate circumference of a circle.
  /*
  int r;
  printf("Enter a radius of circle");
  scanf("%d", &r);
  float c = 2 * 3.14 * r;
  printf("circumference of a circle is %d is %0.2f", r, c);
  */

  // Q3. Write a program to calculate simple interest.
  /*
  int p, r, t;
  printf("Enter a amount\n, rate\n and time\n");
  scanf("%d %d %d", &p, &r, &t);
  float c = p * r * t / 100.0;
  printf("simple interest is %d,%d and %d is %0.2f", p, r, t, c);
  */

  // Q4. write a program to calculate volume of a cuboid.
  /*
  int l, b, h;
  printf("Enter a length\n, breath \nand height");
  scanf("%d %d %d", &l, &b, &h);
  float c = l * b * h;
  printf("volume of a cuboid is %d, %d and %d is %0.2f", l, b, h, c);
  */

  // Q6. write a program to input a character from the user and print its ASCII code.
  /*
  char a;
  printf("Enter a character");
  scanf("%c", &a);
  printf("ASCII code is %c is %d", a, a);
  */

  // Q7. write a program to input an  ASCII code from the user and print with their corresponding codes.
  /*
  int a;
  printf("Enter a ASCII code");
  scanf("%d", &a);
  printf(" character is %d is %c", a, a);
  */

  // Q8. write a program to input a character from the user and print  character with their corresponding ASCII code.
  
  int a, b, c;
  printf("Enter three character");
  scanf("%d %d %d", &a, &b, &c);
  printf("ASCII code is %c is %d", a, a);
  
  return 0;
}